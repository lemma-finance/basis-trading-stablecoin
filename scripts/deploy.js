const hre = require("hardhat");
const { ethers, upgrades } = hre;
const { constants } = ethers;

async function main() {

}
main()
    .then(() => process.exit(0))
    .catch(error => {
        console.error(error);
        process.exit(1);
    });